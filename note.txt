# Shebang #!/bin/bash
# Заголовок через echo
# Вывести дату и время запуска — команда date
# Проверить загрузку CPU — команда top или mpstat
# Проверить память — команда free
# Проверить диск — команда df
#Сравнить значения с порогом (например 80%) — if/else
#Записать результат в лог-файл — оператор >>
ГОТОВО!


Скрипт бэкапа папки
Что будет делать:
    Принимать папку как аргумент $1
    Проверять что папка существует
    Архивировать через tar с датой в имени файла
    Писать результат в лог


Shebang + переменные цветов #!/bin/bash
Проверить что пользователь передал аргумент $1
Проверить что папка существует (-d)
Создать архив через tar с датой в имени
Проверить что архив создался успешно ($?)
Вывести результат и записать в лог



### для себя! Проверяет числа и не остановится 
### пока не будет достигнут конец