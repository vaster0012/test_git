# Shebang
# Заголовок через echo
# Вывести дату и время запуска — команда date
Проверить загрузку CPU — команда top или mpstat
Проверить память — команда free
Проверить диск — команда df
Сравнить значения с порогом (например 80%) — if/else
Записать результат в лог-файл — оператор >>